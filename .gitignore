__pycache__/
*.pyc

# Local secrets — never commit these.
config.json
tokens.json

# Logged-in browser sessions used by the scrape source (scraper.py).
browser-profiles/
last-*.html

# Python virtual environment
.venv/
